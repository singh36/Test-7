// JavaScript's indexOf() method will return the index of the first instance of an element in the array.
let a=[1,2,3,4];
let val=a.indexOf(4);
console.log(val);//output: 3

//  If the element does not exist then, -1 is returned.

let val1=a.indexOf(5);
console.log(val1);// output: -1
















